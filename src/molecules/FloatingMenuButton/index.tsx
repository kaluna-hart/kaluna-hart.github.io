import React from 'react';
import { IconMenu, IconButton } from 'sancho';

export const FloatingMenuButton: React.FCX = (props) => (
  <IconButton>
    <IconMenu />
  </IconButton>
);

export default FloatingMenuButton;
